$NetBSD: patch-ac,v 1.1 2000/12/27 23:44:02 wiz Exp $

--- mbus_parser.c.orig	Tue May 16 19:02:56 2000
+++ mbus_parser.c
@@ -167,7 +167,7 @@
         }
 
 	*i = strtol(m->buffer, &p, 10);
-	if (((*i == LONG_MAX) || (*i == LONG_MIN)) && (errno == ERANGE)) {
+	if (((*i == INT_MAX) || (*i == INT_MIN)) && (errno == ERANGE)) {
 		debug_msg("integer out of range\n");
 		return FALSE;
 	}
