$NetBSD: patch-ak,v 1.1 2006/06/13 07:23:58 dbj Exp $

--- src/libguppidata/guppi-seq-scalar.c.orig	2002-01-14 00:01:17.000000000 -0500
+++ src/libguppidata/guppi-seq-scalar.c
@@ -65,6 +65,8 @@ struct _GuppiSeqScalarPrivate {
   guint save_sum_abs : 1;
 };
 
+static GtkWidget *info_display (GuppiData *);
+
 static GtkObjectClass *parent_class = NULL;
 
 static void
@@ -1775,7 +1777,6 @@ import_xml_element (GuppiSeq *seq, Guppi
 static void
 guppi_seq_scalar_class_init (GuppiSeqScalarClass *klass)
 {
-  static GtkWidget *info_display (GuppiData *);
 
   GtkObjectClass *object_class = (GtkObjectClass *) klass;
   GuppiDataClass *data_class = GUPPI_DATA_CLASS (klass);
