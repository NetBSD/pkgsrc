$NetBSD: patch-src_3rdparty_chromium_services_device_BUILD.gn,v 1.2 2026/04/30 06:39:42 adam Exp $

* Part of patchset to build chromium on NetBSD
* Based on OpenBSD's chromium patches, and
  pkgsrc's qt5-qtwebengine patches

--- src/3rdparty/chromium/services/device/BUILD.gn.orig	2026-03-16 11:40:07.000000000 +0000
+++ src/3rdparty/chromium/services/device/BUILD.gn
@@ -17,7 +17,7 @@ if (is_ios) {
 }
 
 is_serial_enabled_platform =
-    is_win || ((is_linux || is_chromeos) && use_udev) || is_mac || is_android
+    is_win || ((!is_bsd && is_linux || is_chromeos) && use_udev) || is_mac || is_android
 
 source_set("lib") {
   # This should be visible only to embedders of the Device Service, and the
