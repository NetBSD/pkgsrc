$NetBSD: patch-ab,v 1.2 2010/01/15 19:18:42 tnn Exp $

--- configure.ac.orig	2010-01-09 00:01:09.000000000 +0000
+++ configure.ac
@@ -400,11 +400,11 @@ then
 fi
 
 dnl Check for plugin support headers and libraries.
-dnl FIXME: use unstable
+dnl FIXME: use nstable
 if test "x${enable_plugin}" = "xyes"
 then
   PKG_CHECK_MODULES(XULRUNNER, \
-    nspr mozilla-js mozilla-plugin libxul-unstable >= 1.9)
+    mozilla-nspr mozilla-js mozilla-plugin libxul >= 1.9)
   PKG_CHECK_MODULES(GTK, gtk+-2.0)
   IT_XULRUNNER_VERSION
 fi
