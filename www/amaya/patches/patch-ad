$NetBSD: patch-ad,v 1.8 2004/05/13 03:47:29 xtraeme Exp $

--- ../tools/mkdep/mkdep.c.orig	2004-05-13 05:34:59.000000000 +0200
+++ ../tools/mkdep/mkdep.c	2004-05-13 05:35:12.000000000 +0200
@@ -385,7 +385,7 @@
 	res = munmap(mmap_map, (size_t) mmap_mapsize);
         if (res < 0) perror("unmap failed");
 #else
-        mmap_map = malloc(mmap_mapsize);
+        mmap_map = (char *)malloc(mmap_mapsize);
 	if (mmap_map == NULL) {
 	    perror("malloc failed");
 	    exit(1);
