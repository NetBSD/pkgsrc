================================================================================
Web files by default should be placed under:
	${PREFIX}/share/httpd/htdocs

For help use:
	${PREFIX}/sbin/lhs -h
================================================================================
