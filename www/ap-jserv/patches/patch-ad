$NetBSD: patch-ad,v 1.2 2000/02/05 13:59:49 wiz Exp $
--- src/c/Makefile.in.orig	Sat Jan 15 20:09:12 2000
+++ src/c/Makefile.in	Sat Jan 15 20:30:00 2000
@@ -391,7 +391,7 @@
 @MAKE_LIBJSERV_TRUE@	# file is also created (and on Solaris, the .a file isn't created)
 @MAKE_LIBJSERV_TRUE@	# so check for that to make things work.
 @MAKE_LIBJSERV_TRUE@	# HPUX uses .sl
-@MAKE_LIBJSERV_TRUE@	@ if ${TEST} -e ./.libs/${LIBJA} ; then \
+@MAKE_LIBJSERV_TRUE@	@ if false ; then \
 @MAKE_LIBJSERV_TRUE@		echo "${CP} ./.libs/${LIBJA} $(DESTDIR)${LIBEXECDIR}/${MODSO}" ; \
 @MAKE_LIBJSERV_TRUE@		${CP} ./.libs/${LIBJA} $(DESTDIR)${LIBEXECDIR}/${MODSO} ; \
 @MAKE_LIBJSERV_TRUE@        elif ${TEST} -e ./.libs/${LIBJSO} ; then \
