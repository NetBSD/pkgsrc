$NetBSD: patch-third__party_widevine_cdm_widevine.gni,v 1.25 2026/09/02 13:13:38 kikadf Exp $

* Part of patchset to build chromium on NetBSD
* Based on OpenBSD's chromium patches, and
  pkgsrc's qt5-qtwebengine patches

--- third_party/widevine/cdm/widevine.gni.orig	2026-08-31 22:47:51.000000000 +0000
+++ third_party/widevine/cdm/widevine.gni
@@ -26,6 +26,9 @@ if (is_chromeos && !is_chromeos_device) 
 library_widevine_cdm_available =
     (is_chromeos && (target_cpu == "x64" || target_cpu == "arm64")) ||
     (target_os == "linux" && (target_cpu == "x64" || target_cpu == "arm64")) ||
+    (target_os == "openbsd" && target_cpu == "x64") ||
+    (target_os == "freebsd" && target_cpu == "x64") ||
+    (target_os == "netbsd" && target_cpu == "x64") ||
     (target_os == "mac" && (target_cpu == "x64" || target_cpu == "arm64")) ||
     (target_os == "win" &&
      (target_cpu == "x86" || target_cpu == "x64" || target_cpu == "arm64"))
