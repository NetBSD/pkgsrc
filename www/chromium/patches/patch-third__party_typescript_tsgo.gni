$NetBSD: patch-third__party_typescript_tsgo.gni,v 1.1 2026/09/22 13:41:32 kikadf Exp $

* Part of patchset to build chromium on NetBSD
* Based on OpenBSD's chromium patches, and
  pkgsrc's qt5-qtwebengine patches

--- third_party/typescript/tsgo.gni.orig	2026-09-14 22:17:16.000000000 +0000
+++ third_party/typescript/tsgo.gni
@@ -4,7 +4,7 @@
 
 import("//build/toolchain/rbe.gni")
 
-if (host_os == "linux" || use_remoteexec) {
+if (host_os == "linux" || use_remoteexec || host_os == "openbsd" || host_os == "freebsd" || host_os == "netbsd") {
   _platform_dir = "linux-amd64"
   _binary_name = "tsc"
 } else if (host_os == "mac") {
