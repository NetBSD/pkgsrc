$NetBSD: patch-sandbox_policy_mojom_sandbox.mojom,v 1.14 2026/01/19 16:14:18 kikadf Exp $

* Part of patchset to build chromium on NetBSD
* Based on OpenBSD's chromium patches, and
  pkgsrc's qt5-qtwebengine patches

--- sandbox/policy/mojom/sandbox.mojom.orig	2026-01-07 00:50:30.000000000 +0000
+++ sandbox/policy/mojom/sandbox.mojom
@@ -68,6 +68,15 @@ enum Sandbox {
   // Minimally privileged sandbox on Fuchsia.
   [EnableIf=is_fuchsia] kVideoCapture,
 
+  [EnableIf=is_openbsd]
+  kVideoCapture,
+
+  [EnableIf=is_netbsd]
+  kVideoCapture,
+
+  [EnableIf=is_freebsd]
+  kVideoCapture,
+
   // Allows access to file contents and Windows APIs for parsing icons from PE
   // files.
   [EnableIf=is_win] kIconReader,
