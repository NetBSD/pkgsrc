$NetBSD: patch-device_bluetooth_cast__bluetooth.gni,v 1.6 2025/09/08 13:24:26 kikadf Exp $

* Part of patchset to build chromium on NetBSD
* Based on OpenBSD's chromium patches, and
  pkgsrc's qt5-qtwebengine patches

--- device/bluetooth/cast_bluetooth.gni.orig	2025-08-29 18:50:09.000000000 +0000
+++ device/bluetooth/cast_bluetooth.gni
@@ -6,5 +6,5 @@ import("//build/config/features.gni")
 
 declare_args() {
   force_cast_bluetooth = false
-  use_bluez = (is_linux && !is_castos && use_dbus) || is_chromeos
+  use_bluez = use_dbus && !is_bsd
 }
