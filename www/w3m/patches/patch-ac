$NetBSD: patch-ac,v 1.6 2000/08/15 05:01:22 itojun Exp $
repair iso-8859 build.

--- display.c.orig	Tue Jun  6 14:39:13 2000
+++ display.c	Tue Aug 15 13:58:05 2000
@@ -242,3 +242,3 @@
     else
-#if LANG == JA
+#if KANJI_SYMBOL
 	msg = Sprintf("¢ã¢¬¢­Viewing <%s> ", cleanup_str(buf->buffername));
@@ -261,3 +261,3 @@
 		Strcat(msg, Strsubstr(s, 0, (l - 2) / 2));
-#if LANG == JA
+#if KANJI_SYMBOL
 		Strcat_charp(msg, "¡Ä");
@@ -719,4 +719,6 @@
         buf->pos = l->len -1;
+#ifdef JP_CHARSET
         if (CharType(p[buf->pos]) == PC_KANJI2)
             buf->pos--;
+#endif
     }
