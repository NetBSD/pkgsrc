$NetBSD: patch-ad,v 1.12 2003/01/08 01:52:44 jlam Exp $

--- configure.orig	Thu Sep  5 16:40:53 2002
+++ configure
@@ -5396,10 +5396,6 @@ EOF
 
   fi
 
-if test -d /usr/pkg/include -a -d /usr/pkg/lib ; then
-       CPPFLAGS="$CPPFLAGS -I/usr/pkg/include"
-       LDFLAGS="$LDFLAGS -L/usr/pkg/lib"
-fi
 INCLUDES="$INCLUDES -I\$(top_builddir)/Zend"
 test -d /usr/ucblib && 
   if test "/usr/ucblib" != "/usr/lib"; then
@@ -26847,19 +26843,10 @@ EOF
 
 
   else
-    cat >> confdefs.h <<\EOF
-#define HYPERWAVE 0
-EOF
-
     echo "$ac_t""no" 1>&6
   fi
 
 else
-  
-  cat >> confdefs.h <<\EOF
-#define HYPERWAVE 0
-EOF
-
   echo "$ac_t""no" 1>&6
 
 fi
@@ -67729,8 +67716,8 @@ program|static)
     if test "$lib_build_shared" != "yes"; then
         enable_shared=no
     fi
-    PHP_COMPILE='$(CC) $(COMMON_FLAGS) $(CFLAGS_CLEAN) $(EXTRA_CFLAGS) -c $< && touch $@'
-    CXX_PHP_COMPILE='$(CXX) $(COMMON_FLAGS) $(CXXFLAGS_CLEAN) $(EXTRA_CXXFLAGS)  -c $< && touch $@'
+    PHP_COMPILE='$(LIBTOOL) --mode=compile $(COMPILE) -c $<'
+    CXX_PHP_COMPILE='$(LIBTOOL) --mode=compile $(CXX_COMPILE) -c $<'
 ;;
 shared)
     enable_static=no
@@ -72831,7 +72818,7 @@ esac
 
 TSRM_LIB='TSRM/libtsrm.la'
 TSRM_DIR=TSRM
-CPPFLAGS="$CPPFLAGS -I\$(top_builddir)/TSRM"
+INCLUDES="$INCLUDES -I\$(top_builddir)/TSRM"
 
 if test "$ZEND_EXPERIMENTAL_ZTS" = "yes"; then
   cat >> confdefs.h <<\EOF
@@ -73625,7 +73612,7 @@ fi
 
 if test "$enable_debug" != "yes"; then
   
-  LIBTOOL='$(SHELL) $(top_builddir)/libtool --silent'
+  LIBTOOL='$(SHELL) $(top_builddir)/libtool'
 
 fi
 
