$NetBSD: patch-aa,v 1.1 2001/10/07 14:00:44 rh Exp $

--- src/child.c.orig	Mon May 28 21:32:36 2001
+++ src/child.c
@@ -51,6 +51,8 @@
 
 		return *childpid;
 	} else   /* Child */ {
+		char *argp[2];
+
 		close (pipe1[1]);
 		close (pipe2[0]);
 
@@ -59,6 +61,12 @@
 
 		close (pipe1[0]);
 		close (pipe2[1]);
+
+		if (arg == NULL) {
+			argp[0] = cmd;
+			argp[1] = NULL;
+			arg = argp;
+		}
 
 		if (execvp (cmd, arg) < 0)
 			perror (cmd);
