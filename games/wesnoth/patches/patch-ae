$NetBSD: patch-ae,v 1.3 2008/04/19 09:35:24 wiz Exp $

--- utils/mp-server/update_server.orig	2007-12-17 05:26:05.000000000 +0000
+++ utils/mp-server/update_server
@@ -35,7 +35,7 @@ if [ "$2" != "" ]; then
 fi
 echo 'svn update...'
 rev=$(svn up $REVISION | tail -n 1 | sed -e 's/[^0-9]//g')
-if [ "$rev" == "" ]; then
+if [ "$rev" = "" ]; then
 	echo "No revision information found."
 	exit 1
 fi
