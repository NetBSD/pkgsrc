$NetBSD: patch-CMakeLists.txt,v 1.4 2025/08/19 11:52:25 wiz Exp $

Fix build with cmake 4.

--- CMakeLists.txt.orig	2025-08-19 11:50:02.554009370 +0000
+++ CMakeLists.txt
@@ -6,10 +6,6 @@ set(PROJECT_VERSION "1.4")
 
 add_definitions( -DSUPERTUXKART_VERSION="${PROJECT_VERSION}" )
 
-if(NOT (CMAKE_MAJOR_VERSION VERSION_LESS 3))
-  cmake_policy(SET CMP0043 OLD)
-endif()
-
 set (CMAKE_MODULE_PATH "/scratch/games/supertuxkart/work/.buildlink/cmake-Modules" ${CMAKE_MODULE_PATH} "${PROJECT_SOURCE_DIR}/cmake")
 include(CMakeDependentOption)
 
