$NetBSD: patch-aa,v 1.1.1.1 2000/07/14 12:14:49 ad Exp $

--- Makefile.orig	Fri May 19 20:41:10 2000
+++ Makefile	Fri Jul 14 13:07:51 2000
@@ -1,6 +1,6 @@
 # Makefile ispell-gaeilge
 
-ISPELLDIR=/usr/local/lib
+ISPELLDIR=$(PREFIX)/lib
 RELEASE=0.1
 RAWWORDS= gaeilge.raw
 AFFIXFILE= gaeilge.aff
@@ -8,6 +8,8 @@
 APPNAME=ispell-gaeilge-$(RELEASE)
 TARFILE=$(APPNAME).tar
 
+all:	$(HASHFILE)
+
 $(HASHFILE): $(RAWWORDS) $(AFFIXFILE)
 	buildhash $(RAWWORDS) $(AFFIXFILE) $(HASHFILE)
 
@@ -46,4 +48,3 @@
 install: $(HASHFILE)
 	cp $(HASHFILE) $(ISPELLDIR)
 	cp $(AFFIXFILE) $(ISPELLDIR)
-	make reallyclean
