$NetBSD: patch-ad,v 1.12 2013/10/05 11:08:24 tron Exp $

--- icudefs.mk.in.orig	2013-05-23 21:06:32.000000000 +0100
+++ icudefs.mk.in	2013-10-05 12:01:05.000000000 +0100
@@ -207,13 +207,13 @@
 
 ## If we can't use the shared libraries, use the static libraries
 ifneq ($(ENABLE_SHARED),YES)
-STATIC_PREFIX_WHEN_USED = s
+STATIC_PREFIX_WHEN_USED = 
 else
 STATIC_PREFIX_WHEN_USED = 
 endif
 
 # Static library prefix and file extension
-STATIC_PREFIX = s
+STATIC_PREFIX =
 LIBSICU = $(LIBPREFIX)$(STATIC_PREFIX)$(ICUPREFIX)
 A = a
 SOBJ = $(SO)
@@ -306,5 +306,9 @@
 cross_buildroot = $(top_builddir)
 endif
 
+ifeq ($(DSO_LIBDIR),)
+DSO_LIBDIR=	$(DESTDIR)$(libdir)
+endif
+
 # optional include at top
 -include $(top_builddir)/icudefs.local
