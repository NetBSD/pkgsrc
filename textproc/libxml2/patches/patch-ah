$NetBSD: patch-ah,v 1.1 2006/05/05 10:02:18 drochner Exp $

--- relaxng.c.orig	2006-05-05 11:39:00.000000000 +0200
+++ relaxng.c
@@ -7394,7 +7394,7 @@ xmlRelaxNGParse(xmlRelaxNGParserCtxtPtr 
     if (root == NULL) {
         xmlRngPErr(ctxt, (xmlNodePtr) doc,
 	           XML_RNGP_EMPTY, "xmlRelaxNGParse: %s is empty\n",
-                   (ctxt->URL ? ctxt->URL : "schemas"), NULL);
+                   (ctxt->URL ? ctxt->URL : (unsigned char *)"schemas"), NULL);
 	
         xmlFreeDoc(ctxt->document);
         ctxt->document = NULL;
