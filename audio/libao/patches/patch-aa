$NetBSD: patch-aa,v 1.6 2002/01/02 12:23:49 lukem Exp $

--- src/plugins/oss/ao_oss.c.orig	Tue Oct 16 10:13:47 2001
+++ src/plugins/oss/ao_oss.c
@@ -82,8 +82,8 @@
 	char *err = NULL;
 	char *dev = NULL;
 
-	/* default: first try the devfs path */
-	*dev_path = strdup("/dev/sound/dsp");
+	/* default: first try the NetBSD path */
+	*dev_path = strdup("/dev/sound");
 #ifdef BROKEN_OSS
 	fd = open(*dev_path, O_WRONLY | O_NONBLOCK);
 #else
