$NetBSD: patch-ac,v 1.1 2001/08/03 17:07:26 wiz Exp $

--- src/id3tag.c.orig	Mon Jul 16 22:17:08 2001
+++ src/id3tag.c
@@ -1033,25 +1033,5 @@
  */
 ID3_C_EXPORT size_t my_ID3Field_GetASCII(const ID3Field *field, char *buffer, size_t maxChars, index_t itemNum)
 {
-#    if (ID3LIB_MAJOR_VERSION >= 3)
-#        if (ID3LIB_MINOR_VERSION <= 7)
-//#            if (ID3LIB_PATCH_VERSION <= 12)
-                 // (<= to 3.7.12)
-                 return ID3Field_GetASCII(field,buffer,MAX_STRING_LEN,itemNum);
-//                 return ID3Field_GetASCII(field,buffer,MAX_STRING_LEN,1);
-//#            else
-                 // (>= to 3.7.13)
-//                 return ID3Field_GetASCII(field,buffer,MAX_STRING_LEN,0);
-//#            endif
-#        elif (ID3LIB_MINOR_VERSION >= 8)
-             // (>= to 3.8.x)
              return ID3Field_GetASCII(field,buffer,MAX_STRING_LEN);
-//#        else
-             // Not tested (< 3.7.x)
-//             return ID3Field_GetASCII(field,buffer,MAX_STRING_LEN,1);
-#        endif
-#    else
-         // Not tested (< 3.x.x)
-         return ID3Field_GetASCII(field,buffer,MAX_STRING_LEN,itemNum);
-#    endif
 }
