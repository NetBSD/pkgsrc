$NetBSD: patch-ac,v 1.4 1999/08/08 03:47:05 hubertf Exp $

--- Makefile.orig	Thu Sep 19 13:18:29 1996
+++ Makefile	Fri Jul  2 04:54:32 1999
@@ -6,11 +6,13 @@
 
 CC     = gcc
 CFLAGS = -Wall -O2
-LIBS   = -lncurses
+CFLAGS += -I${LOCALBASE}/include	# for ncurses.h
+LDFLAGS+= -L${LOCALBASE}/lib		# for -lncurses
+LIBS   = -lncurses -lossaudio
 INSTALL = install
-BINDIR = /usr/local/bin
+BINDIR = ${PREFIX}/bin
 MANFILE= cam.1
-MANDIR = /usr/man/man1
+MANDIR = ${PREFIX}/man/man1
 EFILE  = cam
 XEFILE = xcam
 OBJS   = cam.o screens.o sounds.o
@@ -24,7 +26,7 @@
 	@echo ""
 	@echo " linking ..."
 	@echo ""
-	$(CC) -o $(EFILE) $(OBJS) $(LIBS)
+	$(CC) $(LDFLAGS) -o $(EFILE) $(OBJS) $(LIBS)
 	@echo ""
 
 ###############################################################################
@@ -68,11 +70,8 @@
 install:$(EFILE) $(MANFILE)
 	@echo ""
 	@echo "installing $(EFILE) in directory $(BINDIR)"
-	@$(INSTALL) -m 755 -o root -g root $(EFILE) $(BINDIR)
-	@$(INSTALL) -m 755 -o root -g root $(XEFILE) $(BINDIR)
+	@$(BSD_INSTALL_PROGRAM) $(EFILE) $(BINDIR)
 	@echo ""
 	@echo "installing manpages for cam"
-	@$(INSTALL) -m 644 -o root -g root $(MANFILE) $(MANDIR)
-	@echo ""
-	@. installvgafont.sh
+	@$(BSD_INSTALL_DATA) $(MANFILE) $(MANDIR)
 	@echo ""
