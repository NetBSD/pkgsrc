$NetBSD: patch-configure.ac,v 1.5 2026/06/07 19:27:00 wiz Exp $

--- configure.ac.orig	2026-06-01 08:04:24.000000000 +0000
+++ configure.ac
@@ -774,6 +774,15 @@ AC_OUTPUT
 
 AC_OUTPUT
 
+case $host_os in
+  netbsd*|freebsd*|dragonfly*)
+    HAVE_LIBDL=yes
+cat >>confdefs.h <<\_ACEOF
+#define HAVE_LIBDL 1
+_ACEOF
+    ;;
+esac
+
 dnl Create asoundlib.h dynamically according to configure options
 echo "Creating asoundlib.h..."
 cp "$srcdir"/include/asoundlib-head.h include/asoundlib.h
