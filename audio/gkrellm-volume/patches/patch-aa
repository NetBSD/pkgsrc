$NetBSD: patch-aa,v 1.3 2004/02/24 22:39:26 cube Exp $

--- Makefile.orig	2003-01-18 20:59:09.000000000 +0100
+++ Makefile
@@ -2,14 +2,14 @@
 
 GTK_CONFIG = pkg-config gtk+-2.0
 
-PLUGIN_DIR ?= /usr/local/lib/gkrellm2/plugins
-GKRELLM_INCLUDE = -I/usr/local/include
+PLUGIN_DIR ?= $(PREFIX)/lib/gkrellm2/plugins
+GKRELLM_INCLUDE = -I$(BUILDLINK_DIR)/include
 
 GTK_CFLAGS = `$(GTK_CONFIG) --cflags` 
 GTK_LIB = `$(GTK_CONFIG) --libs`
 
 FLAGS = -O2 -Wall -fPIC $(GTK_CFLAGS) $(GKRELLM_INCLUDE)
-LIBS = $(GTK_LIB)
+LIBS = $(GTK_LIB) $(LIBOSSAUDIO)
 LFLAGS = -shared
 
 PACKAGE ?= gkrellm-volume
