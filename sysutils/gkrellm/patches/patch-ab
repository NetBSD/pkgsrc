$NetBSD: patch-ab,v 1.6 2001/11/01 14:08:02 hubertf Exp $

--- src/Makefile.src.orig	Fri Oct 12 15:01:04 2001
+++ src/Makefile.src
@@ -1,10 +1,10 @@
 GTK_CONFIG ?= gtk-config
 BINMODE ?= 755
-INSTALLDIR ?= /usr/local/bin
-INCLUDEDIR ?= /usr/local/include
+INSTALLDIR ?= ${PREFIX}/bin
+INCLUDEDIR ?= ${PREFIX}/include
 INCLUDEMODE ?= 644
 INCLUDEDIRMODE ?= 755
-MANDIR ?= /usr/local/share/man/man1
+MANDIR ?= ${PREFIX}/man/man1
 MANMODE ?= 644
 MANDIRMODE ?= 755
 INSTALL ?= install
@@ -13,15 +13,18 @@
 GKRELLM_INCLUDES = gkrellm.h gkrellm_public_proto.h
 
 GTK_INCLUDE = `$(GTK_CONFIG) --cflags gthread`
-IMLIB_INCLUDE = `imlib-config --cflags-gdk`
+IMLIB_INCLUDE = `$(IMLIB_CONFIG) --cflags-gdk`
 
 GTK_LIB = `$(GTK_CONFIG) --libs gthread`
-IMLIB_LIB = `imlib-config --libs-gdk`
+IMLIB_LIB = `$(IMLIB_CONFIG) --libs-gdk`
 
-FLAGS = -O2 -I.. $(GTK_INCLUDE) $(IMLIB_INCLUDE) $(GTOP_INCLUDE)
+FLAGS = -I.. $(GTK_INCLUDE) $(IMLIB_INCLUDE) $(GTOP_INCLUDE)
 FLAGS+= $(PTHREAD_INC)
 
-LIBS = $(GTK_LIB) $(IMLIB_LIB) $(GTOP_LIBS) $(SYS_LIBS)
+FLAGS+= -DSYSTEM_PLUGINS_DIR=\"${PREFIX}/lib/gkrellm/plugins\"
+FLAGS+= -DSYSTEM_THEMES_DIR=\"${PREFIX}/share/gkrellm/themes\"
+
+LIBS = $(GTK_LIB) $(IMLIB_LIB) $(GTOP_LIBS) $(SYS_LIBS) -Wl,-E
 
 ifeq ($(debug),1)
     FLAGS += -g
@@ -36,7 +39,7 @@
 endif
 
 
-CC = gcc -Wall $(FLAGS) $(CFLAGS)
+CC += -Wall $(FLAGS) $(CFLAGS)
 
 OBJS =	main.o alerts.o apm.o clock.o cpu.o disk.o fs.o hostname.o \
 	inet.o mail.o meminfo.o net.o proc.o sensors.o uptime.o \
@@ -59,8 +62,8 @@
 		 SYS_LIBS="-lkvm -ldevstat" gkrellm
 
 netbsd:
-	$(MAKE) PTHREAD_INC=-I/usr/local/include \
-	GTK_CONFIG=gtk-config GTOP_LIBS= SYS_LIBS="-lkvm" gkrellm
+	$(MAKE) PTHREAD_INC=-I$(BUILDLINK_DIR)/include \
+	GTK_CONFIG=$(GTK_CONFIG) GTOP_LIBS= SYS_LIBS="-lkvm" gkrellm
 
 solaris:
 	$(MAKE) CFLAGS="-Wno-implicit-int" \
