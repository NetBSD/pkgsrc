$NetBSD: patch-aa,v 1.5 2001/10/30 13:31:48 damon Exp $

--- Makefile.top.orig	Sat Oct 20 11:51:29 2001
+++ Makefile.top
@@ -74,11 +74,11 @@
 GTOP_LIBS = -L$(GTOP_PREFIX)/lib -lgtop -lgtop_common -lgtop_sysdeps -lXau
 
 
-INSTALLDIR=/usr/local/bin
-MANDIR=/usr/local/share/man/man1
+INSTALLDIR=${PREFIX}/bin
+MANDIR=${PREFIX}/man/man1
 MANMODE = 644
 MANDIRMODE = 755
-INCLUDEDIR=/usr/local/include
+INCLUDEDIR=${PREFIX}/include
 INCLUDEMODE = 644
 INCLUDEDIRMODE = 755
 
@@ -127,8 +127,8 @@
 
 netbsd:
 	(cd po && ${MAKE} all)
-	(cd src && ${MAKE} GTK_CONFIG=gtk-config GTOP_LIBS= GTOP_INCLUDE= \
-		PTHREAD_INC=-I/usr/local/include SYS_LIBS="-lkvm" gkrellm )
+	(cd src && ${MAKE} GTK_CONFIG=${GTK_CONFIG} GTOP_LIBS= GTOP_INCLUDE= \
+		PTHREAD_INC=-I$(BUILDLINK_DIR)/include SYS_LIBS="-lkvm" gkrellm )
 
 openbsd:
 	(cd po && ${MAKE} all)
