$NetBSD: patch-Makefile,v 1.1 2024/10/10 17:59:19 nia Exp $

Honour PKGMANDIR.

--- Makefile.orig	2024-10-10 17:57:44.340636734 +0000
+++ Makefile
@@ -6,7 +6,6 @@ LIBTREE_DEFINES = -D_FILE_OFFSET_BITS=64
 
 PREFIX ?= /usr/local
 BINDIR ?= $(PREFIX)/bin
-SHAREDIR ?= $(PREFIX)/share
 
 .PHONY: all check install clean
 
@@ -22,8 +21,8 @@ libtree: $(libtree-objs)
 install: all
 	mkdir -p $(DESTDIR)$(BINDIR)
 	cp -p libtree $(DESTDIR)$(BINDIR)
-	mkdir -p $(DESTDIR)$(SHAREDIR)/man/man1
-	cp -p doc/libtree.1 $(DESTDIR)$(SHAREDIR)/man/man1
+	mkdir -p $(DESTDIR)$(PREFIX)/$(PKGMANDIR)/man1
+	cp -p doc/libtree.1 $(DESTDIR)$(PREFIX)/$(PKGMANDIR)/man1
 
 check:: libtree
 
