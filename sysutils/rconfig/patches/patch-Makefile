$NetBSD: patch-Makefile,v 1.1 2017/08/19 00:25:07 jlam Exp $

--- Makefile.orig	2009-02-11 22:56:54.000000000 +0000
+++ Makefile
@@ -15,6 +15,7 @@ INSTALL_PROGRAM ?= install -m 0755
 INSTALL_SCRIPT	?= install -m 0755
 SYSCONFDIR	?= ${PREFIX}/etc
 PREFIX 		?= /usr/local
+PKGMANDIR	?= man
 
 LIBEXEC		= ${PREFIX}/libexec
 
@@ -39,8 +40,8 @@ install: ${PKGNAME}.${MANSECTION}
 	${INSTALL_DIR} ${DESTDIR}${LIBEXEC}
 	${INSTALL_SCRIPT} probe.sh ${DESTDIR}${LIBEXEC}/rconfig_probe
 	${INSTALL_SCRIPT} ssh.sh ${DESTDIR}${LIBEXEC}/rconfig_ssh
-	${INSTALL_DIR} ${DESTDIR}${PREFIX}/man/man${MANSECTION}
-	${INSTALL_MAN} ${PKGNAME}.${MANSECTION} ${DESTDIR}${PREFIX}/man/man${MANSECTION}
+	${INSTALL_DIR} ${DESTDIR}${PREFIX}/${PKGMANDIR}/man${MANSECTION}
+	${INSTALL_MAN} ${PKGNAME}.${MANSECTION} ${DESTDIR}${PREFIX}/${PKGMANDIR}/man${MANSECTION}
 
 # GENFILES
 
