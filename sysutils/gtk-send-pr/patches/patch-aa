$NetBSD: patch-aa,v 1.2 2004/04/20 19:18:04 minskim Exp $

--- Makefile.orig	2003-11-22 12:36:43.000000000 -0600
+++ Makefile
@@ -5,7 +5,7 @@ LOCALBASE?=/usr/local
 
 GTK_CFLAGS!=pkg-config gtk+-2.0 --cflags
 ESMTP_CFLAGS!=libesmtp-config --cflags
-CFLAGS+= -Wall -g -DGTK_DISABLE_DEPRECATED ${GTK_CFLAGS} ${ESMTP_CFLAGS}
+CFLAGS+= -Wall -g ${GTK_CFLAGS} ${ESMTP_CFLAGS}
 
 GTK_LDFLAGS!=pkg-config gtk+-2.0 --libs
 ESMTP_LDFLAGS!=libesmtp-config --libs
@@ -20,6 +20,9 @@ OBJECTS= main.o gtk_ui.o user_settings.o
 all:	$(OBJECTS)
 	$(CC) $(LDFLAGS) -o gtk-send-pr $(OBJECTS)
 
+install:
+	${BSD_INSTALL_PROGRAM} gtk-send-pr ${PREFIX}/bin
+	${BSD_INSTALL_MAN} gtk-send-pr.1 ${PREFIX}/man/man1
+
 clean:
 	rm -f *.o gtk-send-pr gtk-send-pr.core
-
