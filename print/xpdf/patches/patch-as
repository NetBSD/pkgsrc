$NetBSD: patch-as,v 1.1.2.2 2005/08/13 05:27:41 snj Exp $

Fix for CAN-2005-2097, from Ubuntu and RedHat.

--- xpdf/SplashOutputDev.cc.orig	2004-01-22 02:26:45.000000000 +0100
+++ xpdf/SplashOutputDev.cc	2005-08-12 13:54:26.000000000 +0200
@@ -621,16 +621,19 @@
       }
       break;
     case fontTrueType:
-      if (!(ff = FoFiTrueType::load(fileName->getCString()))) {
-	goto err2;
-      }
+      if ((ff = FoFiTrueType::load(fileName->getCString()))) {
       codeToGID = ((Gfx8BitFont *)gfxFont)->getCodeToGIDMap(ff);
+	n = 256;
       delete ff;
+      } else {
+	codeToGID = NULL;
+	n = 0;
+      }
       if (!(fontFile = fontEngine->loadTrueTypeFont(
 			   id,
 			   fileName->getCString(),
 			   fileName == tmpFileName,
-			   codeToGID, 256))) {
+			   codeToGID, n))) {
 	error(-1, "Couldn't create a font for '%s'",
 	      gfxFont->getName() ? gfxFont->getName()->getCString()
 	                         : "(unnamed)");
