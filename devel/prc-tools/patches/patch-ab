$NetBSD: patch-ab,v 1.1 2002/10/15 23:07:17 dillo Exp $

--- gcc/configure.in.orig	Tue Aug 27 10:43:24 2002
+++ gcc/configure.in
@@ -422,7 +422,8 @@ if [ x"${with_headers}" != x ]; then
   "") x=${prefix} ;;
   *) x=${exec_prefix} ;;
   esac
-  copy_dirs="${copy_dirs} ${with_headers} $x/${target_alias}/sys-include"
+  # XXX: directories under ${prefix} are not user writable, duh!
+  #copy_dirs="${copy_dirs} ${with_headers} $x/${target_alias}/sys-include"
 fi
 
 # Handle --with-libs=XXX.  Multiple directories are permitted.  The
