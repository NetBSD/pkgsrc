$NetBSD: patch-ao,v 1.1 2004/02/26 16:25:53 cube Exp $

--- src/attach_process.c.orig	Thu Jan 15 19:44:13 2004
+++ src/attach_process.c
@@ -170,10 +170,11 @@ iter_stack_push_new (AttachProcess *ap, 
 static gboolean
 iter_stack_pop (AttachProcess *ap)
 {
+	GtkTreeIter *iter;
 	if (ap->priv->iter_stack_level < 0)
 		return FALSE;
 
-	GtkTreeIter *iter =
+	iter =
 			(GtkTreeIter *) (g_slist_nth_data (ap->priv->iter_stack, 0));
 	ap->priv->iter_stack =
 			g_slist_delete_link (ap->priv->iter_stack, ap->priv->iter_stack);
