$NetBSD: patch-ah,v 1.1 2005/01/09 17:30:00 jmmv Exp $

--- glib/gutils.c.orig	2005-01-03 20:57:20.000000000 +0100
+++ glib/gutils.c
@@ -1857,7 +1857,7 @@ g_get_system_data_dirs (void)
       data_dirs = (gchar *) g_getenv ("XDG_DATA_DIRS");
 
       if (!data_dirs || !data_dirs[0])
-          data_dirs = "/usr/local/share/:/usr/share/";
+          data_dirs = PREFIX "/share/";
 #endif
       data_dir_vector = g_strsplit (data_dirs, G_SEARCHPATH_SEPARATOR_S, 0);
 
@@ -1900,7 +1900,7 @@ g_get_system_config_dirs (void)
       conf_dirs = (gchar *) g_getenv ("XDG_CONFIG_DIRS");
 
       if (!conf_dirs || !conf_dirs[0])
-          conf_dirs = "/etc/xdg";
+          conf_dirs = PKG_SYSCONFDIR "/xdg";
 #endif
       conf_dir_vector = g_strsplit (conf_dirs, G_SEARCHPATH_SEPARATOR_S, 0);
 
@@ -1975,7 +1975,7 @@ unalias_lang (char *lang)
   int i;
 
   if (!alias_table)
-    read_aliases ("/usr/share/locale/locale.alias");
+    read_aliases (PREFIX "/" PKGLOCALEDIR "/locale/locale.alias");
 
   i = 0;
   while ((p = g_hash_table_lookup (alias_table, lang)) && (strcmp (p, lang) != 0))
