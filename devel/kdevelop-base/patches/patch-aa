$NetBSD: patch-aa,v 1.5 2004/04/20 14:15:41 markd Exp $

--- configure.orig	2004-04-06 02:57:52.000000000 +1200
+++ configure
@@ -38961,7 +38961,7 @@ ac_link='rm -rf SunWS_cache; ${CXX-g++} 
 
     save_CXXFLAGS="$CXXFLAGS"
 
-    CXXFLAGS="$CXXFLAGS $X_CFLAGS -ldb"
+    CXXFLAGS="$CXXFLAGS $X_CFLAGS -ldb4"
 
     cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -40101,9 +40101,9 @@ fi
 
   test -n "$TAR" && break
 done
-test -n "$TAR" || TAR="
-tar=${tar-"${am_missing_run}"}
-"
+#test -n "$TAR" || TAR="
+#tar=${tar-"${am_missing_run}"}
+#"
 
 
 cat >>confdefs.h <<_ACEOF
