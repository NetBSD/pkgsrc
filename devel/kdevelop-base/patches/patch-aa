$NetBSD: patch-aa,v 1.6 2004/08/21 15:39:02 markd Exp $

--- configure.orig	2004-08-14 12:07:07.000000000 +1200
+++ configure
@@ -40078,9 +40078,9 @@ fi
 
   test -n "$TAR" && break
 done
-test -n "$TAR" || TAR="
-tar=${tar-"${am_missing_run}"}
-"
+#test -n "$TAR" || TAR="
+#tar=${tar-"${am_missing_run}"}
+#"
 
 for ac_prog in gzip
 do
@@ -40121,9 +40121,9 @@ fi
 
   test -n "$GZIP" && break
 done
-test -n "$GZIP" || GZIP="
-gzip=${gzip-"${am_missing_run}"}
-"
+#test -n "$GZIP" || GZIP="
+#gzip=${gzip-"${am_missing_run}"}
+#"
 
 
 cat >>confdefs.h <<_ACEOF
