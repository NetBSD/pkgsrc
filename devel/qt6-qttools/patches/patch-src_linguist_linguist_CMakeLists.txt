$NetBSD: patch-src_linguist_linguist_CMakeLists.txt,v 1.2 2025/11/11 12:34:17 adam Exp $

On Darwin, do not build application bundle.

--- src/linguist/linguist/CMakeLists.txt.orig	2025-10-01 22:59:56.000000000 +0000
+++ src/linguist/linguist/CMakeLists.txt
@@ -289,7 +289,7 @@ if(QT_INSTALL_XDG_DESKTOP_ENTRIES)
     endif()
 endif()
 
-if(APPLE)
+if(FALSE)
     set_target_properties(linguist PROPERTIES
         MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/Info_mac.plist"
         MACOSX_BUNDLE TRUE
@@ -302,7 +302,7 @@ if(APPLE)
     target_sources(linguist PRIVATE
         linguist.icns
     )
-else()
+elif(NOT APPLE)
     set_property(SOURCE main.cpp PROPERTY SKIP_AUTOMOC ON)
 endif()
 qt_internal_add_docs(linguist
