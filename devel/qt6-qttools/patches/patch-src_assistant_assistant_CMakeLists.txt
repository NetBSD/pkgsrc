$NetBSD: patch-src_assistant_assistant_CMakeLists.txt,v 1.2 2025/11/11 12:34:17 adam Exp $

On Darwin, do not build application bundle.

--- src/assistant/assistant/CMakeLists.txt.orig	2025-10-01 22:59:56.000000000 +0000
+++ src/assistant/assistant/CMakeLists.txt
@@ -181,7 +181,7 @@ if(QT_INSTALL_XDG_DESKTOP_ENTRIES)
     endif()
 endif()
 
-if(APPLE)
+if(FALSE)
     set_target_properties(assistant PROPERTIES
         MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/Info_mac.plist"
         MACOSX_BUNDLE TRUE
