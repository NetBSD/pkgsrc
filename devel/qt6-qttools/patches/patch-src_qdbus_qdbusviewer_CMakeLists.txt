$NetBSD: patch-src_qdbus_qdbusviewer_CMakeLists.txt,v 1.2 2025/11/11 12:34:17 adam Exp $

On Darwin, do not build application bundle.

--- src/qdbus/qdbusviewer/CMakeLists.txt.orig	2025-10-01 22:59:56.000000000 +0000
+++ src/qdbus/qdbusviewer/CMakeLists.txt
@@ -56,7 +56,7 @@ if(QT_INSTALL_XDG_DESKTOP_ENTRIES)
     endif()
 endif()
 
-if(APPLE)
+if(FALSE)
     set_target_properties(qdbusviewer PROPERTIES
         MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/Info_mac.plist"
         MACOSX_BUNDLE TRUE
