$NetBSD: patch-ac,v 1.3 2004/09/26 19:29:16 danw Exp $

--- bonobo-activation/bonobo-activation-client.c.orig	Mon Sep 13 10:26:19 2004
+++ bonobo-activation/bonobo-activation-client.c
@@ -197,7 +197,7 @@ get_lang_list (void)
         return result;
 }
 
-#if defined(__APPLE__) && defined(HAVE_NSGETENVIRON) && defined(HAVE_CRT_EXTERNS_H)
+#if defined(__APPLE__)
 #include <crt_externs.h>
 #define environ (*_NSGetEnviron())
 #endif
