$NetBSD: patch-ac,v 1.2 2003/03/06 08:51:01 jmc Exp $

--- gal/e-table/e-table-group.c.orig	Mon Aug 19 15:35:16 2002
+++ gal/e-table/e-table-group.c	Thu Mar  6 08:11:22 2003
@@ -129,13 +129,15 @@
 			 ETableHeader     *header,
 			 ETableModel      *model)
 {
+	va_list ap;
 	etg->full_header = full_header;
 	gtk_object_ref (GTK_OBJECT(etg->full_header));
 	etg->header = header;
 	gtk_object_ref (GTK_OBJECT(etg->header));
 	etg->model = model;
 	gtk_object_ref (GTK_OBJECT(etg->model));
-	gnome_canvas_item_construct (GNOME_CANVAS_ITEM (etg), parent, 0, NULL);
+	/* Safe to pass uninitialized ap since 0 property arg won't eval it. */
+	gnome_canvas_item_construct (GNOME_CANVAS_ITEM (etg), parent, 0, ap);
 }
 
 /**
