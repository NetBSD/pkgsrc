$NetBSD: patch-ae,v 1.2 2025/10/09 11:02:56 wiz Exp $

--- configure.orig	2004-03-01 23:06:18.000000000 +0000
+++ configure
@@ -758,7 +758,7 @@ cat > conftest.$ac_ext << EOF
 #line 759 "configure"
 #include "confdefs.h"
 
-main(){return(0);}
+int main(){return(0);}
 EOF
 if { (eval echo configure:764: \"$ac_link\") 1>&5; (eval $ac_link) 2>&5; } && test -s conftest${ac_exeext}; then
   ac_cv_prog_cc_works=yes
@@ -1422,8 +1422,8 @@ fi
 if test "x$have_tk" != "xno" ; then
 	tklibflag=`eval echo $TK_LIB_FLAG`
 	TCL_CPPFLAGS="-I$tk_h_dir $TK_XINCLUDES $TCL_CPPFLAGS"
-	TCL_LDFLAGS="-L$tk_lib_dir $TCL_LDFLAGS"
-	TCL_LIBS="$tklibflag $TCL_LIBS"
+	TCL_LDFLAGS="$TK_LIB_SPEC $TCL_LDFLAGS"
+	TCL_LIBS="$tklibflag $TCL_LIBS $TK_LIBS"
 	cat >> confdefs.h <<\EOF
 #define HAVE_TK 1
 EOF
