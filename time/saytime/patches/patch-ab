$NetBSD: patch-ab,v 1.5 2025/12/18 13:10:10 nia Exp $

--- saytime.c.orig	2015-01-27 20:15:03.000000000 +0000
+++ saytime.c
@@ -24,6 +24,7 @@
 ** SUCH DAMAGE.
 */
 
+#include <fcntl.h>
 #include <unistd.h>
 #include <stdlib.h>
 #include <stdio.h>
@@ -502,10 +503,10 @@ main( int argc, char** argv )
 int
 main( int argc, char** argv )
     {
-    long clock;
+    time_t clock;
     struct tm* t;
 
-    clock = time( (long*) 0 );
+    clock = time( (time_t*) 0 );
     t = localtime( &clock );
 
     sayphrase( PH_THE_TIME_IS );
