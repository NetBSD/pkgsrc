$NetBSD: patch-aa,v 1.1 2011/01/31 20:25:51 wiz Exp $

Fix build with png-1.5.

--- src/loaders/png.c.orig	2007-09-16 11:01:50.000000000 +0000
+++ src/loaders/png.c
@@ -110,7 +110,7 @@ read_png(const char * filename,
 		goto err;
 	}
 
-	if (setjmp(png_ptr->jmpbuf)) {
+	if (setjmp(png_jmpbuf(png_ptr))) {
 		png_destroy_read_struct(&png_ptr, &info_ptr,
 					(png_infopp) NULL);
 		msg("libpng error catched");
