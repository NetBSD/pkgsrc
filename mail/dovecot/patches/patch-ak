$NetBSD: patch-ak,v 1.1 2007/02/05 20:31:14 ghen Exp $

From dovecot CVS, to fix "dovecot -a" segfault:
http://dovecot.org/pipermail/dovecot/2007-February/019246.html

--- src/master/master-settings-defs.c.orig	2007-02-02 12:14:31.000000000 +0100
+++ src/master/master-settings-defs.c
@@ -96,7 +96,7 @@ static struct setting_def setting_defs[]
 	DEF(SET_STR, mail_plugins),
 	DEF(SET_STR, mail_plugin_dir),
 	DEF(SET_STR, mail_log_prefix),
-	DEF(SET_STR, mail_log_max_lines_per_sec),
+	DEF(SET_INT, mail_log_max_lines_per_sec),
 
 	/* imap */
 	DEF(SET_INT, imap_max_line_length),
