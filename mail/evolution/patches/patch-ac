$NetBSD: patch-ac,v 1.3 2001/09/29 17:43:56 rh Exp $

--- shell/main.c.orig	Tue Sep 18 01:21:34 2001
+++ shell/main.c
@@ -60,6 +60,23 @@
 static gboolean no_splash = FALSE;
 extern char *evolution_debug_log;
 
+#ifdef __NetBSD__
+#include <gtk/gtk.h>
+#include <pthread.h>
+
+/*
+ * NetBSD uses non-preemptive pth, so we yield the processor periodically
+ */
+
+gint
+pth_nbschedule (gpointer data)
+{
+	pthread_yield_np();
+
+	return TRUE;
+}
+#endif
+
 
 static GtkWidget *
 quit_box_new (void)
@@ -347,6 +364,10 @@
 	}
 
 	gtk_idle_add (idle_cb, uri_list);
+
+#ifdef __NetBSD__
+	gtk_timeout_add (200, pth_nbschedule, NULL);
+#endif
 
 	bonobo_main ();
 
