$NetBSD: patch-aa,v 1.20 2006/04/01 16:13:56 jmmv Exp $

--- configure.orig	2006-04-01 13:05:40.000000000 +0200
+++ configure
@@ -29653,13 +29653,10 @@ fi
 echo "$as_me:$LINENO: checking for command to kill processes" >&5
 echo $ECHO_N "checking for command to kill processes... $ECHO_C" >&6
 
-if test `uname -s` = "SunOS" ; then
-	KILL_PROCESS_CMD="pkill";
-else
-	KILL_PROCESS_CMD="killall";
+KILL_PROCESS_CMD=`which pkill`
+if test -z "$KILL_PROCESS_CMD" ; then
+	KILL_PROCESS_CMD=`which killall`
 fi
-
-KILL_PROCESS_CMD=`which $KILL_PROCESS_CMD`
 if test -z "$KILL_PROCESS_CMD" ; then
 	echo "$as_me:$LINENO: result: none" >&5
 echo "${ECHO_T}none" >&6
