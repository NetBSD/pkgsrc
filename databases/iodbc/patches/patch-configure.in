$NetBSD: patch-configure.in,v 1.2 2014/08/21 19:38:55 wiz Exp $

--- configure.in.orig	2014-04-15 11:00:45.000000000 +0000
+++ configure.in
@@ -820,6 +820,7 @@ ${B}Installation variables${N}
   exec_prefix		${exec_prefix}
 
 ${B}Installation paths${N}
+echo "AC_HELP_STRING(pkgconfig, 	[${pkgconfigdir}])"
   programs		${bindir}
   include files		${includedir}
   libraries		${libdir}
