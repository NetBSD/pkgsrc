$NetBSD: patch-aj,v 1.4 2005/07/03 15:23:34 wulf Exp $

--- doc/Makefile.in.orig	2005-05-28 22:33:22.000000000 +0930
+++ doc/Makefile.in	2005-06-19 13:58:04.000000000 +0930
@@ -228,9 +228,9 @@
 
 # some install rules if the install pat
 DOCPREFIX := $(shell echo "@prefix@" | grep rrd >/dev/null && echo "rrdtool-@VERSION@/")
-idocdir = $(prefix)/shared/doc/$(DOCPREFIX)txt
+idocdir = $(prefix)/share/doc/rrdtool
 idoc_DATA = $(POD) $(TXT)
-ihtmldir = $(prefix)/shared/doc/$(DOCPREFIX)html
+ihtmldir = $(prefix)/share/doc/rrdtool/html
 ihtml_DATA = $(HTML)
 imandir = $(mandir)/man1
 iman_DATA = $(MAN)
@@ -247,9 +247,9 @@
 	      exit 1;; \
 	  esac; \
 	done; \
-	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu  doc/Makefile'; \
+	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign  doc/Makefile'; \
 	cd $(top_srcdir) && \
-	  $(AUTOMAKE) --gnu  doc/Makefile
+	  $(AUTOMAKE) --foreign  doc/Makefile
 .PRECIOUS: Makefile
 Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
 	@case '$?' in \
