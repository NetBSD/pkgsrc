$NetBSD: patch-ah,v 1.3 2026/08/04 14:59:11 ryoon Exp $

Use standard headers
Fix build with GCC 14.

--- dba.c.orig	2026-08-04 12:10:25.465394876 +0000
+++ dba.c
@@ -2,15 +2,24 @@
  * dba	dbm analysis/recovery
  */
 
+#include <fcntl.h>
 #include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <unistd.h>
 #include <sys/file.h>
 #include "sdbm.h"
 
 char *progname;
 extern void oops();
 
+void sdump();
+int pagestat();
+int okpage();
+
 int
 main(argc, argv)
+int argc;
 char **argv;
 {
 	int n;
@@ -36,14 +45,15 @@ char **argv;
 	return 0;
 }
 
+void
 sdump(pagf)
 int pagf;
 {
-	register b;
-	register n = 0;
-	register t = 0;
-	register o = 0;
-	register e;
+	register int b;
+	register int n = 0;
+	register int t = 0;
+	register int o = 0;
+	register int e;
 	char pag[PBLKSIZ];
 
 	while ((b = read(pagf, pag, PBLKSIZ)) > 0) {
@@ -66,11 +76,12 @@ int pagf;
 		oops("read failed: block %d", n);
 }
 
+int
 pagestat(pag)
 char *pag;
 {
-	register n;
-	register free;
+	register int n;
+	register int free;
 	register short *ino = (short *) pag;
 
 	if (!(n = ino[0]))
