$NetBSD: patch-ab,v 1.6 2008/06/21 17:26:53 wiz Exp $

Don't force a build of the PO files, they need newer gettext versions.

--- Makefile.am.orig	2008-06-11 18:24:50.000000000 +0000
+++ Makefile.am
@@ -60,8 +60,6 @@ uninstall-hook:
 	rm -f "$(DESTDIR)$(datadir)/mime/generic-icons"
 	rm -f "$(DESTDIR)$(datadir)/mime/globs2"
 
-all: check
-
 check:
 	@if test -n $(XMLLINT) ; then \
 		xmllint --noout --valid $(srcdir)/freedesktop.org.xml;	\
