$NetBSD: patch-aa,v 1.2 2026/03/23 10:12:50 nia Exp $

--- Xju.c.orig	2026-03-23 09:47:44.787227079 +0000
+++ Xju.c
@@ -5,3 +5,4 @@
  */
-#include <varargs.h>
+#include <stdio.h>
+#include <stdarg.h>
 #include <X11/Intrinsic.h>
@@ -11,4 +12,3 @@ extern Widget root;
 
-void SetArgs(va_alist)
-va_dcl
+void SetArgs(Arg **arglist, ...)
 {
@@ -23,9 +23,6 @@ va_dcl
 	unsigned i;
-	Arg **arglist;
 	Cardinal *count;
 
-	va_start(argindex);
-
 	/* Get first two arguments */
-	arglist = va_arg(argindex, Arg **);
+	va_start(argindex, arglist);
 	count = va_arg(argindex, Cardinal *);
@@ -46,6 +43,5 @@ va_dcl
 	/* Set new Arg list */
-	va_start(argindex);
+	va_start(argindex, arglist);
 
 	/* skip past first two arguments */
-	(void) va_arg(argindex, Arg **);
 	(void) va_arg(argindex, int *);
@@ -60,4 +56,3 @@ va_dcl
 
-Widget CreateWidget(va_alist) 
-va_dcl
+Widget CreateWidget(char *name, ...) 
 {
@@ -80,9 +75,7 @@ va_dcl
 	Widget w, parent;
-	char *name;
 	WidgetClass class;
 
-	va_start(argindex);
+	va_start(argindex, name);
 
 	/* Get first three arguments */
-	name = va_arg(argindex, char *);
 	class = va_arg(argindex, WidgetClass);
@@ -104,6 +97,5 @@ va_dcl
 	/* Set new Arg list */
-	va_start(argindex);
+	va_start(argindex, name);
 
 	/* skip past first three arguments */
-	(void) va_arg(argindex, char *);
 	(void) va_arg(argindex, WidgetClass);
@@ -125,4 +117,3 @@ va_dcl
 	
-void QueryWidget(va_alist)
-va_dcl
+void QueryWidget(Widget w, ...)
 {
@@ -140,8 +131,4 @@ va_dcl
 	Cardinal count;
-	Widget w;
-
-	va_start(argindex);
 
-	/* Get first argument */
-	w = va_arg(argindex, Widget);
+	va_start(argindex, w);
 
@@ -161,6 +148,3 @@ va_dcl
 	/* Set new Arg list */
-	va_start(argindex);
-
-	/* skip past first argument */
-	(void) va_arg(argindex, Widget);
+	va_start(argindex, w);
 
@@ -176,4 +160,3 @@ va_dcl
 	
-void ConfigureWidget(va_alist)
-va_dcl
+void ConfigureWidget(Widget w, ...)
 {
@@ -191,8 +174,4 @@ va_dcl
 	Cardinal count;
-	Widget w;
 
-	va_start(argindex);
-
-	/* Get first argument */
-	w = va_arg(argindex, Widget);
+	va_start(argindex, w);
 
@@ -212,6 +191,3 @@ va_dcl
 	/* Set new Arg list */
-	va_start(argindex);
-
-	/* skip past first argument */
-	(void) va_arg(argindex, Widget);
+	va_start(argindex, w);
 
