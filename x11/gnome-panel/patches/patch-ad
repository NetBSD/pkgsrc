$NetBSD: patch-ad,v 1.1.1.1 2002/11/04 08:28:00 rh Exp $

--- applets/fish/Makefile.in.orig	Sun Sep 15 16:18:36 2002
+++ applets/fish/Makefile.in
@@ -166,7 +166,7 @@ FISH_BITMAPS =  	fishanim.png		oldwanda.
 EXTRA_DIST =  	$(FISH_BITMAPS)					GNOME_FishApplet_Factory.server.in.in		gnome-fish.png					fish.schemas.in					$(ui_DATA)
 
 
-fishbitmapsdir = $(datadir)/pixmaps/fish
+fishbitmapsdir = $(datadir)/pixmaps/fish2
 fishbitmaps_DATA = $(FISH_BITMAPS)
 
 pixmapdir = $(datadir)/pixmaps
@@ -321,12 +321,13 @@ install-pixmapDATA: $(pixmap_DATA)
 	@$(NORMAL_INSTALL)
 	$(mkinstalldirs) $(DESTDIR)$(pixmapdir)
 	@list='$(pixmap_DATA)'; for p in $$list; do \
+	inst=`echo $$p | sed s/gnome/gnome2/` ; \
 	  if test -f $(srcdir)/$$p; then \
 	    echo " $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pixmapdir)/$$p"; \
-	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pixmapdir)/$$p; \
+	    $(INSTALL_DATA) $(srcdir)/$$p $(DESTDIR)$(pixmapdir)/$$inst; \
 	  else if test -f $$p; then \
-	    echo " $(INSTALL_DATA) $$p $(DESTDIR)$(pixmapdir)/$$p"; \
-	    $(INSTALL_DATA) $$p $(DESTDIR)$(pixmapdir)/$$p; \
+	    echo " $(INSTALL_DATA) $$p $(DESTDIR)$(pixmapdir)/$$inst"; \
+	    $(INSTALL_DATA) $$p $(DESTDIR)$(pixmapdir)/$$inst; \
 	  fi; fi; \
 	done
 
