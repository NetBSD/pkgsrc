$NetBSD: patch-al,v 1.7 2004/12/10 06:05:16 markd Exp $

--- kdm/kfrontend/config.def.orig	Sun Oct  3 20:11:07 2004
+++ kdm/kfrontend/config.def
@@ -53,6 +53,8 @@
 # define DEF_SERVER_LINE ":0 local@console " XBINDIR "/X -nolisten tcp"
 #elif defined(_AIX)
 # define DEF_SERVER_LINE ":0 local@lft0 " XBINDIR "/X -T -force -nolisten tcp"
+#elif defined(__NetBSD__)
+# define DEF_SERVER_LINE ":0 local " XBINDIR "/X -nolisten tcp vt05"
 #else
 # define DEF_SERVER_LINE ":0 local " XBINDIR "/X -nolisten tcp"
 #endif
