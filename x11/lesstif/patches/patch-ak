$NetBSD: patch-ak,v 1.11 2000/12/22 17:26:19 jlam Exp $

--- clients/Motif-2.1/xmbind/Makefile.am.orig	Fri Nov 24 09:58:22 2000
+++ clients/Motif-2.1/xmbind/Makefile.am
@@ -4,8 +4,12 @@
 
 if Version_2_1
 bin_PROGRAMS=	xmbind
-endif
+if DefaultVersion21
+bindir = $(prefix)/bin
+else
 bindir = $(prefix)/LessTif/Motif2.1/bin
+endif
+endif
 
 MAINTAINERCLEANFILES=Makefile.in
 
@@ -57,10 +61,3 @@
 	      rm $$i; \
 	   fi ; \
 	   done
-
-install-data-hook:
-if DefaultVersion21
-	-rm -f $(DESTDIR)$(prefix)/bin/$(bin_PROGRAMS)
-	(cd $(DESTDIR)$(prefix)/bin && $(LN_S) ../LessTif/Motif2.1/bin/$(bin_PROGRAMS) .)
-endif
-
