$NetBSD: patch-bd,v 1.3 2006/01/23 22:54:44 joerg Exp $

--- programs/Xserver/hw/xfree86/os-support/bsd/bsd_init.c.orig	2006-01-23 22:47:05.000000000 +0000
+++ programs/Xserver/hw/xfree86/os-support/bsd/bsd_init.c
@@ -245,6 +245,7 @@ xf86OpenConsole()
 	     * switching anymore. Here we check for FreeBSD 3.1 and up.
 	     * Add cases for other *BSD that behave the same.
 	    */
+#if !defined(__DragonFly__)
 #if defined(__FreeBSD__) || defined(__FreeBSD_kernel__)
 	    uname (&uts);
 	    i = atof(uts.release) * 100;
@@ -266,6 +267,7 @@ xf86OpenConsole()
 		}
 		sleep(1);
 	    }
+#endif
 
 acquire_vt:
 	    /*
