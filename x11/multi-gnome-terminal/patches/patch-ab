$NetBSD: patch-ab,v 1.3 2005/12/15 16:53:08 joerg Exp $

--- gnome-terminal/vt.c.orig	2005-12-15 16:45:11.000000000 +0000
+++ gnome-terminal/vt.c
@@ -782,6 +782,7 @@ static void vt_pl(struct vt_em *vt)
       vt_up(vt);    
       break;
     default:
+      break;
     }
 
 }
@@ -806,6 +807,7 @@ static void vt_nl(struct vt_em *vt)
       vt_down(vt);
       break;
     default:
+      break;
     }
 }
 
@@ -870,6 +872,7 @@ vt_tab_clear_cur(struct vt_em *vt)
 	}
       break; 
     default:
+      break;
     }
 }
 
@@ -953,6 +956,7 @@ static void vt_backtab(struct vt_em *vt)
 	} 
       break;
     default:
+      break;
     }
     /*vt->cursorx = (vt->cursorx-1) & (~7);*/
 }
@@ -974,6 +978,7 @@ void vt_tab_index(struct vt_em *vt)
 	} 
      break; 
     default:
+     break;
     } 
 }
 
@@ -2067,9 +2072,11 @@ vt_deccharmode (struct vt_em *vt)
 */	      
 	break;
 	default:
+          break;
 	}
       break;
 	default:
+          break;
     } 
   /* do nothing!*/
 }
