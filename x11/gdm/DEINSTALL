# $NetBSD: DEINSTALL,v 1.2 2003/01/10 17:26:55 jmmv Exp $

if [ "$2" != POST-DEINSTALL ]
then
	cd @GDMCFGDIR@
	@RM@ -f Sessions/Default
	for SOURCE in @GDMCFGFILES@
	do
		TARGET=${SOURCE%.default*}${SOURCE##*.default}
		if cmp -s $SOURCE $TARGET
		then
			@RM@ -f $TARGET
		fi
	done
fi
