$NetBSD: patch-ae,v 1.1 2001/05/03 18:37:49 fredb Exp $

--- main.c.orig	Tue Nov 28 04:44:05 2000
+++ main.c
@@ -1508,7 +1508,8 @@
 	if (ioctl (pty, FIONBIO, (char *)&mode) == -1) SysError (ERROR_FIONBIO);
 #endif	/* USE_SYSV_TERMIO */
 	
-#if defined(linux)||defined(SVR4)||(defined(hpux)&&OSMAJORVERSION>=10)
+
+#ifndef VMS
 	pty_mask = pty;
 	X_mask = Xsocket;
 	FD_ZERO(&Select_mask);
