$NetBSD: patch-configure,v 1.3 2026/05/09 20:26:47 vins Exp $

Allow ntfs-3g on NetBSD.

--- configure.orig	2026-05-09 18:01:25.850768653 +0000
+++ configure
@@ -15268,7 +15268,7 @@ elif test "x${with_fuse}" = "x"; then
 	{ printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking fuse compatibility" >&5
 printf %s "checking fuse compatibility... " >&6; }
 	case "${target_os}" in
-	linux*|solaris*)
+	netbsd*|linux*|solaris*)
 
 # Check whether --with-fuse was given.
 if test ${with_fuse+y}
