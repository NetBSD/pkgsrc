$NetBSD: patch-ab,v 1.2 2002/06/26 18:02:40 agc Exp $

--- Makefile.orig	Wed Apr  3 06:32:18 2002
+++ Makefile	Wed Jun 26 19:00:37 2002
@@ -88,6 +88,7 @@
 	route.c vif.c timer.c mrt.c pim6.c pim6_proto.c rp.c crc.c trace.c\
 	cfparse.y cftoken.l
 CLEANFILES+= lex.yy.c y.tab.c y.tab.h
+YFLAGS+=-d
 
 .if (${OPSYS} == "BSD/OS")
 CC=	gcc
@@ -127,6 +128,12 @@
 
 realinstall:
 	install ${COPY} -o ${BINOWN} -g ${BINGRP} -m ${BINMODE} \
-		${INSTALLFLAGS} pim6sd ${.CURDIR}/pim6stat ${DESTDIR}${BINDIR}
+		${INSTALLFLAGS} ${.CURDIR}/pim6stat ${DESTDIR}${BINDIR}
 
 .include <bsd.prog.mk>
+
+.if ${OPSYS} == "NetBSD"
+.y.c:
+	${YACC.y} ${.IMPSRC}
+	mv y.tab.c ${.TARGET}
+.endif
