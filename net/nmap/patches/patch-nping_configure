$NetBSD: patch-nping_configure,v 1.2 2026/09/14 22:10:12 tnn Exp $

pcap_version is not portable; use pcap_lib_version(), which returns a
string like "libpcap version 1.10.7 (...)", so skip to the first digit
before parsing it.

--- nping/configure.orig	2025-05-15 09:53:51.037421707 +0000
+++ nping/configure
@@ -6057,9 +6057,12 @@ else $as_nop
 /* end confdefs.h.  */
 
 #include <stdio.h>
-extern char pcap_version[];
+#include <pcap/pcap.h>
 int main() {
-  int major, minor1, minor2;
+  int major = 0, minor1 = 0, minor2 = 0;
+  const char *pcap_version = pcap_lib_version();
+  while (*pcap_version != '\0' && (*pcap_version < '0' || *pcap_version > '9'))
+    pcap_version++;
   sscanf(pcap_version,"%d.%d.%d", &major, &minor1, &minor2);
   if (major > 0)
     return 0;
@@ -6095,9 +6098,12 @@ else $as_nop
 #ifdef HAVE_NET_BPF_H
 #include <net/bpf.h>
 #endif
-extern char pcap_version[];
+#include <pcap/pcap.h>
 int main() {
-  int major, minor;
+  int major = 0, minor = 0;
+  const char *pcap_version = pcap_lib_version();
+  while (*pcap_version != '\0' && (*pcap_version < '0' || *pcap_version > '9'))
+    pcap_version++;
   sscanf(pcap_version,"%d.%d", &major, &minor);
   if ((major == 1 && minor >= 1) || major > 1)
     return 0;
