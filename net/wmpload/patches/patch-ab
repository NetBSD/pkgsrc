$NetBSD: patch-ab,v 1.2 2001/07/22 16:52:30 wiz Exp $

--- Imakefile.orig	Sat Dec 18 02:35:12 1999
+++ Imakefile
@@ -4,23 +4,19 @@
 XCOMM
 
 include osdefs.mk
-	DEPLIBS = $(DEPXLIB)
+        DEPLIBS = $(DEPXLIB)
 LOCAL_LIBRARIES = $(XPMLIB) $(XLIB)
 SYS_LIBRARIES   = MathLibrary
         HEADERS = wmpload.h
-            VER	= 0.9.4
+            VER = 0.9.4
         DEFINES = -DVERSION="\"$(VER)\"" $(OSDEFS)
 
-ComplexProgramTarget(wmpload)
+ComplexProgramTargetNoMan(wmpload)
 
-distclean: clean	
-	$(RM) Makefile osdefs.mk core
-
-tgz:	
+tgz:
 	rm -rf wmpload-$(VER)
 	mkdir wmpload-$(VER)
 	cp `echo * | sed s,wmpload-$(VER),,` wmpload-$(VER)
 	cd wmpload-$(VER) && make distclean
 	tar cvf - wmpload-$(VER) | gzip -9 > wmpload-$(VER).tar.gz
 	rm -rf wmpload-$(VER)
-	
