#! /bin/sh
#
# $NetBSD: DEINSTALL,v 1.1.2.1 2002/06/23 18:54:57 jlam Exp $

case ${STAGE} in
POST-DEINSTALL)
	for file in au coda-setup-ports codaconfedit; do
		${RM} -f ${PKG_PREFIX}/sbin/${file}
		if [ -f ${PKG_PREFIX}/sbin/vice-${file} ]; then
			${LN} -sf vice-${file} ${PKG_PREFIX}/sbin/${file}
		fi
	done
	;;
esac
