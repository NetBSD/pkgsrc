#! /bin/sh
#
# $NetBSD: INSTALL,v 1.1.2.1 2002/06/23 18:54:58 jlam Exp $

case ${STAGE} in
POST-INSTALL)
	for file in au coda-setup-ports codaconfedit; do
		if [ ! -f ${PKG_PREFIX}/sbin/${file} ]; then
			${LN} -sf venus-${file} ${PKG_PREFIX}/sbin/${file}
		fi
	done
	;;
esac
