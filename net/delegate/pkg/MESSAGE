**********************************************************************
Now, edit the sample script in ${PREFIX}/etc/rc.d/delegated.sh.sample
and copy it into ${PREFIX}/etc/rc.d/delegated.sh.
Don't forget to specify a correct e-mail address for MANAGER.

You can also run an interactive configuration session by executing 
delegated with no argument.

For more configuration information, consult delegated(8).
**********************************************************************
