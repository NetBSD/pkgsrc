$NetBSD: patch-aa,v 1.7 2003/10/14 23:46:46 hubertf Exp $

--- Makefile.in.orig	Sat Sep  6 15:15:17 2003
+++ Makefile.in
@@ -55,11 +55,10 @@ install:
 		make installonly
 
 installonly:
-		mkdir -p $(root)/bin/
-		install -m 755 ipv6calc/ipv6calc $(root)/bin/
-		mkdir -p $(root)/usr/bin/
-		install -m 755 ipv6logconv/ipv6logconv $(root)/usr/bin/
-		install -m 755 ipv6logstats/ipv6logstats $(root)/usr/bin/
+		${BSD_INSTALL_PROGRAM_DIR} $(PREFIX)/bin
+		${BSD_INSTALL_PROGRAM} ipv6calc/ipv6calc $(PREFIX)/bin
+		${BSD_INSTALL_PROGRAM} ipv6logconv/ipv6logconv $(PREFIX)/bin
+		${BSD_INSTALL_PROGRAM} ipv6logstats/ipv6logstats $(PREFIX)/bin
 
 getopt-make:
 		cd getopt/ && make
