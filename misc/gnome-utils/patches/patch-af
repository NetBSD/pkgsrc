$NetBSD: patch-af,v 1.3 2001/09/30 22:13:45 mycroft Exp $

--- logview/logrtns.c.orig	Thu Jun 28 08:14:22 2001
+++ logview/logrtns.c	Sun Sep 30 22:10:01 2001
@@ -24,6 +24,9 @@
 #include "config.h"
 #include "logview.h"
 #include "logrtns.h"
+#ifdef HAVE_LOCALE_H
+#include <locale.h>
+#endif
 
 /*
  * -----------------
@@ -405,8 +408,8 @@
 {
    LogLine *line;
    FILE *fp;
-   char *c, ch, buffer[R_BUF_SIZE + 1];
-   int ln;
+   char *c, buffer[R_BUF_SIZE + 1];
+   int ch, ln;
    long int old_pos;
 
    fp = lg->fp;
@@ -476,8 +479,8 @@
 {
    FILE *fp;
    LogLine *line;
-   char *c, ch, buffer[R_BUF_SIZE + 1];
-   int ln, len;
+   char *c, buffer[R_BUF_SIZE + 1];
+   int ch, ln, len;
 
    g_return_val_if_fail (lg != NULL, FALSE);
 
