$NetBSD: patch-af,v 1.2 2001/04/07 20:52:45 rh Exp $

--- logview/logrtns.c.orig	Tue Feb 27 09:33:19 2001
+++ logview/logrtns.c
@@ -294,8 +294,8 @@
 {
    LogLine *line;
    FILE *fp;
-   char *c, ch, buffer[R_BUF_SIZE + 1];
-   int ln;
+   char *c, buffer[R_BUF_SIZE + 1];
+   int ch, ln;
    long int old_pos;
 
    fp = lg->fp;
@@ -365,8 +365,8 @@
 {
    FILE *fp;
    LogLine *line;
-   char *c, ch, buffer[R_BUF_SIZE + 1];
-   int ln, len;
+   char *c, buffer[R_BUF_SIZE + 1];
+   int ch, ln, len;
 
    g_return_val_if_fail (lg != NULL, FALSE);
 
