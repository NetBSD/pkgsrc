$NetBSD: patch-aa,v 1.3 2004/08/21 13:01:33 markd Exp $

--- configure.orig	2004-08-14 11:45:15.000000000 +1200
+++ configure
@@ -32300,12 +32300,12 @@ done
 IFS=$old_IFS
 
 
-   echo "$as_me:$LINENO: checking for lib$kdelibsuff/xscreensaver/flame" >&5
-echo $ECHO_N "checking for lib$kdelibsuff/xscreensaver/flame... $ECHO_C" >&6
+   echo "$as_me:$LINENO: checking for libexec$kdelibsuff/xscreensaver/flame" >&5
+echo $ECHO_N "checking for libexec$kdelibsuff/xscreensaver/flame... $ECHO_C" >&6
    if test -n "$XSCREENSAVER"; then
         kde_cv_path="$XSCREENSAVER";
    else
-        kde_cache=`echo lib$kdelibsuff/xscreensaver/flame | sed 'y%./+-%__p_%'`
+        kde_cache=`echo libexec$kdelibsuff/xscreensaver/flame | sed 'y%./+-%__p_%'`
 
         if eval "test \"\${kde_cv_path_$kde_cache+set}\" = set"; then
   echo $ECHO_N "(cached) $ECHO_C" >&6
@@ -32324,16 +32324,16 @@ else
 	IFS=$kde_save_IFS
 
         for dir in $dirs; do
-	  if test -x "$dir/lib$kdelibsuff/xscreensaver/flame"; then
+	  if test -x "$dir/libexec$kdelibsuff/xscreensaver/flame"; then
 	    if test -n ""
 	    then
-              evalstr="$dir/lib$kdelibsuff/xscreensaver/flame  2>&1 "
+              evalstr="$dir/libexec$kdelibsuff/xscreensaver/flame  2>&1 "
 	      if eval $evalstr; then
-                kde_cv_path="$dir/lib$kdelibsuff/xscreensaver/flame"
+                kde_cv_path="$dir/libexec$kdelibsuff/xscreensaver/flame"
                 break
 	      fi
             else
-		kde_cv_path="$dir/lib$kdelibsuff/xscreensaver/flame"
+		kde_cv_path="$dir/libexec$kdelibsuff/xscreensaver/flame"
                 break
 	    fi
           fi
@@ -32354,12 +32354,12 @@ fi
 echo "${ECHO_T}not found" >&6
 
 
-   echo "$as_me:$LINENO: checking for lib/xscreensaver/flame" >&5
-echo $ECHO_N "checking for lib/xscreensaver/flame... $ECHO_C" >&6
+   echo "$as_me:$LINENO: checking for libexec/xscreensaver/flame" >&5
+echo $ECHO_N "checking for libexec/xscreensaver/flame... $ECHO_C" >&6
    if test -n "$XSCREENSAVER"; then
         kde_cv_path="$XSCREENSAVER";
    else
-        kde_cache=`echo lib/xscreensaver/flame | sed 'y%./+-%__p_%'`
+        kde_cache=`echo libexec/xscreensaver/flame | sed 'y%./+-%__p_%'`
 
         if eval "test \"\${kde_cv_path_$kde_cache+set}\" = set"; then
   echo $ECHO_N "(cached) $ECHO_C" >&6
@@ -32378,16 +32378,16 @@ else
 	IFS=$kde_save_IFS
 
         for dir in $dirs; do
-	  if test -x "$dir/lib/xscreensaver/flame"; then
+	  if test -x "$dir/libexec/xscreensaver/flame"; then
 	    if test -n ""
 	    then
-              evalstr="$dir/lib/xscreensaver/flame  2>&1 "
+              evalstr="$dir/libexec/xscreensaver/flame  2>&1 "
 	      if eval $evalstr; then
-                kde_cv_path="$dir/lib/xscreensaver/flame"
+                kde_cv_path="$dir/libexec/xscreensaver/flame"
                 break
 	      fi
             else
-		kde_cv_path="$dir/lib/xscreensaver/flame"
+		kde_cv_path="$dir/libexec/xscreensaver/flame"
                 break
 	    fi
           fi
