$NetBSD: patch-ap,v 1.2 2003/12/27 14:35:16 mrg Exp $

--- ksim/monitors/cpu/ksimcpu.cpp.orig	2003-09-02 11:49:19.000000000 +0000
+++ ksim/monitors/cpu/ksimcpu.cpp	2003-12-27 14:28:38.000000000 +0000
@@ -277,11 +277,14 @@
   cpu.idle = cpuTime[CP_IDLE];
 #endif
 
+#if defined(Q_OS_NETBSD)
+#define KERN_CPTIME KERN_CP_TIME
+#endif
 #if defined(Q_OS_OPENBSD) || defined(Q_OS_NETBSD)
 #warning "add support for SMP on OpenBSD and NetBSD"
   int name2oid[2] = { CTL_KERN, KERN_CPTIME };
   long cpuTime[CPUSTATES];
-  unsigned int cpuTimeLen = sizeof(cpuTime);
+  size_t cpuTimeLen = sizeof(cpuTime);
 
   if (sysctl(name2oid, 2, &cpuTime, &cpuTimeLen,
      0, 0) < 0)
