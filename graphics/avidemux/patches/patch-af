$NetBSD: patch-af,v 1.1.1.1 2003/08/02 15:51:41 jmmv Exp $

--- adm_lavcodec/dsputil.h.orig	2003-05-29 00:39:00.000000000 +0200
+++ adm_lavcodec/dsputil.h
@@ -30,7 +30,7 @@
 #include "common.h"
 #include "avcodec.h"
 
-#ifdef __FreeBSD__
+#if defined(__FreeBSD__) || defined(__NetBSD__) || defined(__OpenBSD__)
 #define lrintf rintf
 #endif
 
@@ -476,6 +476,7 @@ static int name16(void /*MpegEncContext*
           +name8(s, dst+8+8*stride, src+8+8*stride, stride);\
 }
 
+#if 0
 #ifndef HAVE_LRINTF
 /* XXX: add ISOC specific test to avoid specific BSD testing. */
 /* better than nothing implementation. */
@@ -490,5 +491,6 @@ static inline long int lrintf(float x)
 #endif
 }
 #endif
+#endif
 
 #endif
