$NetBSD: patch-am,v 1.4 2002/12/05 21:44:31 drochner Exp $

--- src/uipp/dxuilib/PacketIF.C.orig	Sat Mar  2 06:04:55 2002
+++ src/uipp/dxuilib/PacketIF.C	Thu Dec  5 20:29:34 2002
@@ -1429,7 +1429,7 @@
     int fd;
     int sts;
     int oldPort;
-#if !defined(linux) && !defined(cygwin) && !defined(freebsd) && !defined(macos) && !defined(solaris)
+#if !defined(linux) && !defined(cygwin) && !defined(freebsd) && !defined(macos) && !defined(solaris) && !defined(__NetBSD__)
     extern int errno;
 #endif
     int tries;
