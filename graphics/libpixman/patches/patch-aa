$NetBSD: patch-aa,v 1.4 2005/11/04 17:56:38 tv Exp $

--- src/pixman.h.orig	2005-06-25 14:03:42.000000000 -0400
+++ src/pixman.h
@@ -83,12 +83,13 @@ SOFTWARE.
 
 #if defined (__SVR4) && defined (__sun)
 # include <sys/int_types.h>
-#else
-# if defined (__OpenBSD__) || defined (_AIX)
-#  include <inttypes.h>
-# else 
-#  include <stdint.h>
-# endif
+#elif defined(__OpenBSD__) || defined (_AIX)
+# include <inttypes.h>
+#elif defined(__INTERIX)
+# include <sys/types.h>
+# define uint64_t u_int64_t
+#else 
+# include <stdint.h>
 #endif
 
 
