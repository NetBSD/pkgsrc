$NetBSD: patch-bin_epstopdf,v 1.5 2025/05/08 11:57:09 markd Exp $

Fix paths to commands: gs and kpathsea.

--- bin/epstopdf.orig	2024-07-12 21:01:34.000000000 +0000
+++ bin/epstopdf
@@ -272,9 +272,9 @@ debug "Normalized PATH to: @pdirs";
 
 # Default values for commands.
 # Ghostscript, which is always needed.
-my $GS = 'gs';
+my $GS = '@GHOSTSCRIPT@';
 # Kpsewhich, only used in restricted mode.
-my $kpsewhich = 'kpsewhich';
+my $kpsewhich = '@KPSEWHICH@';
 
 # Determine if we were invoked from a standard Windows TL installation via the
 # wrapper script epstopdf.exe:
