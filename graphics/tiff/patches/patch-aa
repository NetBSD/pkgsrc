$NetBSD: patch-aa,v 1.11 2002/03/05 10:33:35 martti Exp $

--- configure.orig	Thu Nov 15 18:21:05 2001
+++ configure	Mon Mar  4 16:02:34 2002
@@ -487,7 +487,7 @@
 
 checkCompiler()
 {
-    compiler=$1
+    compiler="$*"
     if isGNU $compiler; then
 	ISGCC=yes
     else
@@ -513,7 +513,7 @@
     $RM dummy dummy.o
     capture $compiler -o dummy ${C_ANSI} dummy.c && {
 	CC=$compiler;
-	test -z "${CCOMPILER-}" && CCOMPILER=`findApp $compiler $PATH`
+	test -z "${CCOMPILER-}" && CCOMPILER=$compiler
 	test -z "${ENVOPTS-}"   && ENVOPTS="${C_ANSI-}"
 	return 0
     }
@@ -1705,6 +1705,7 @@
 	    -[LR]/*)	echo "$i" ;;
 	    -L*)	echo "$i" | sed 's;^-L;-L../;' ;;
 	    -R*)	echo "$i" | sed 's;^-R;-R../;' ;;
+	    -W*)	echo "$i" ;;
 	    *)		echo "../$i" ;;
 	    esac
 	done) | tr '\012' ' '
