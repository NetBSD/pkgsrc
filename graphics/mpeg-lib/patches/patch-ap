$NetBSD: patch-ap,v 1.3 1998/08/07 10:40:49 agc Exp $

--- Makefile.in.orig	Mon Feb 26 13:12:54 1996
+++ Makefile.in	Sun Jul 26 10:25:54 1998
@@ -6,7 +6,7 @@
 
 # Various programs and their options
 
-CC            = @CC@
+CC            = ${LIBTOOL} @CC@
 OPT           = @OPT@
 DEFS          = @DEFS@
 INCLUDES      = -I.
@@ -31,7 +31,7 @@
 
 # Other files of interest
 
-LIBRARY       = libmpeg.a
+LIBRARY       = libmpeg.la
 MPEGTEST      = mpegtest
 
 # Targets for all platforms
@@ -39,8 +39,8 @@
 all:		$(LIBRARY) @extras@
 
 $(LIBRARY):	$(LIBOBJ)
-		$(AR) $(ARFLAGS) $(LIBRARY) $(LIBOBJ)
-		$(RANLIB) $(LIBRARY)
+		${CC} -o ${LIBRARY} ${LIBOBJ:.o=.lo} -rpath ${PREFIX}/lib \
+			-version-info 1:2
 
 $(LIBOBJ):	Makefile
 
