$NetBSD: patch-ap,v 1.4 2001/07/01 22:00:53 jlam Exp $

--- Makefile.in.orig	Mon Feb 26 13:12:54 1996
+++ Makefile.in
@@ -6,11 +6,11 @@
 
 # Various programs and their options
 
-CC            = @CC@
+CC            = ${LIBTOOL} @CC@
 OPT           = @OPT@
 DEFS          = @DEFS@
 INCLUDES      = -I.
-CFLAGS        = $(OPT) $(DEFS) $(INCLUDES)
+CFLAGS        += $(OPT) $(DEFS) $(INCLUDES)
 AR            = @AR@
 ARFLAGS       = ru
 RANLIB        = @RANLIB@
@@ -31,7 +31,7 @@
 
 # Other files of interest
 
-LIBRARY       = libmpeg.a
+LIBRARY       = libmpeg.la
 MPEGTEST      = mpegtest
 
 # Targets for all platforms
@@ -39,8 +39,8 @@
 all:		$(LIBRARY) @extras@
 
 $(LIBRARY):	$(LIBOBJ)
-		$(AR) $(ARFLAGS) $(LIBRARY) $(LIBOBJ)
-		$(RANLIB) $(LIBRARY)
+		${CC} -o ${LIBRARY} ${LIBOBJ:.o=.lo} -rpath ${PREFIX}/lib \
+			-version-info 1:2
 
 $(LIBOBJ):	Makefile
 
