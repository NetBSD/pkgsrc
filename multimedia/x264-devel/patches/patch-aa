$NetBSD: patch-aa,v 1.3 2008/01/04 18:41:54 joerg Exp $

--- configure.orig	2007-12-18 22:45:04.000000000 +0100
+++ configure
@@ -193,6 +193,10 @@ case $host_os in
     SYS="FREEBSD"
     LDFLAGS="$LDFLAGS -lm"
     ;;
+  dragonfly*)
+    SYS="DRAGONFLY"
+    LDFLAGS="$LDFLAGS -lm"
+    ;;
   kfreebsd*-gnu)
     SYS="FREEBSD"
     CFLAGS="$CFLAGS -DHAVE_MALLOC_H"
@@ -252,7 +256,7 @@ case $host_cpu in
     fi
     as_check || AS="nasm"
     ;;
-  x86_64)
+  x86_64|amd64)
     ARCH="X86_64"
     AS="yasm"
     if [ "$SYS" = MACOSX ];then
