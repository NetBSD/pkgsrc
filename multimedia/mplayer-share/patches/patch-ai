$NetBSD: patch-ai,v 1.3 2007/01/25 18:31:16 drochner Exp $

--- libmpdvdkit2/dvd_reader.c.orig	2007-01-24 22:25:53.000000000 +0100
+++ libmpdvdkit2/dvd_reader.c
@@ -549,10 +549,12 @@ static int findDirFile( const char *path
             sprintf( filename, "%s%s%s", path,
                      ( ( path[ strlen( path ) - 1 ] == '/' ) ? "" : "/" ),
                      ent->d_name );
+	    closedir( dir );
             return 0;
         }
     }
 
+    closedir( dir );
     return -1;
 }
 
