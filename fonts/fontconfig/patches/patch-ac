$NetBSD: patch-ac,v 1.17 2026/07/23 14:47:04 ryoon Exp $

--- conf.d/Makefile.in.orig	2026-07-09 11:50:30.085991600 +0000
+++ conf.d/Makefile.in
@@ -408,7 +408,7 @@ CLEANFILES = $(DOC_FILES)
 	70-$(PREFERRED_BITMAP).conf $(am__append_1)
 EXTRA_DIST = $(template_DATA) 05-macos.conf $(DOC_SOURCES)
 CLEANFILES = $(DOC_FILES)
-configdir = $(CONFIGDIR)
+configdir = $(prefix)/share/examples/fontconfig/conf.d
 config_DATA = $(DOC_FILES)
 templatedir = $(TEMPLATEDIR)
 template_DATA = 05-reset-dirs-sample.conf \
@@ -688,6 +688,7 @@ install-data-hook:
 	cd ../fc-lang && $(MAKE) $(AM_MAKEFLAGS) $(top_builddir)/conf.d/35-lang-normalize.conf
 
 install-data-hook:
+xinstall-data-hook:
 	$(PYTHON) $(srcdir)/link_confs.py $(templatedir) $(configdir) $(CONF_LINKS)
 uninstall-local:
 	@(echo cd $(DESTDIR)$(configdir);			\
