===========================================================================
$NetBSD: MESSAGE,v 1.3 2014/06/01 16:35:35 wiz Exp $

To use the fonts you have just installed, add
the line:

    FontPath ${PREFIX}/share/fonts/X11/jmk

to your X window configuration file, or run the
following command:

    xset +fp ${PREFIX}/share/fonts/X11/jmk
    xset fp rehash

===========================================================================
