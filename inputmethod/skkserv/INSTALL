# $NetBSD: INSTALL,v 1.2 2003/08/30 20:22:55 jlam Exp $

case "$2" in
POST-INSTALL)
	if egrep '^skkserv[ 	]' /etc/services >/dev/null; then
		:
	else
		echo '
skkserv		1178/tcp		# SKK server' >>/etc/services
	fi
	;;
esac
